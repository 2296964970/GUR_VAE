
## 暂缓项（记录但当前不引入）
以下两项属于“可能的后续增强”，当前用户明确暂不引入（当前阶段仍以**基于测量值的修复/补全**为主，不假设隐变量 `z` 与系统状态量存在一一对应关系）。

1) **引入 LGSSM 结构化后验（Kalman filter/smoother 口径）**  
   将 `q(z_{1:T}|x)` 从“逐时间步独立高斯”升级为带一阶马尔可夫结构的高斯后验，以便更严格地对齐 LGSSM 先验的变分推断与不确定性传播。

2) **引入“隐变量/系统状态量”的显式映射或约束**  
   例如将 `latent_dim` 设计为系统状态维度（如 `2n`），并通过物理一致的观测/动力学约束或（未来可能出现的）状态真值监督，使 `z` 具备更强的“状态量语义”。当前暂无状态真值，且不在本阶段范围内。
